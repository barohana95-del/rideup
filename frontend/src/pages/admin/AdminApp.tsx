// Admin app — פאנל ניהול לקוח. שלב 5.
export default function AdminApp({ host: _host }: { host: { hostname: string } }) {
  return (
    <div dir="rtl" className="min-h-screen flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-3xl font-bold mb-2">פאנל ניהול</h1>
        <p className="text-gray-500">בבנייה — שלב 5</p>
      </div>
    </div>
  );
}
